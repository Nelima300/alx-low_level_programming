#include "main.h"

/**
*_isupper - Checks for uppercase character.
*@c: 1 if character to be checked.
*
* Return: 1 if character is uppercase.
*/
int _isupper(int c)
{
	if ((c >= 'A') && (C <= 'Z'))
		return (1)

	else
		return(0);
}
